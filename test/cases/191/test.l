
namespace Test191 is
    class Main is
        void init() is
            var i = 10;
	    var a = new int[][10];
            var p = `test`;

	    IO.Std.err.println( "i is a " + i.box.getClass() );
	    IO.Std.err.println( "a is a " + a.getClass() );
	    IO.Std.err.println( "p is a " + p.box.getClass() );	    
	si
    si
si